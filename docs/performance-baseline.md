# Engine performance baseline — v2.6.0 (W761)

Generated by `pnpm bench` on 2026-09-12T04:35:19.791Z. Machine-readable twin: `celestea-studio-ts.bench-baseline/1` in `benchmarks/baseline-v2.6.0.json`.

The baseline pins the cost of the engine's hot paths so a later regression can be argued with numbers: the W755 statusline tick (which now
performs a full context snapshot per tick), the token estimator and the trim pass, the session-log projection, and SSE envelope encoding/decoding.

## Machine

| fact | value |
| --- | --- |
| CPU | Intel(R) Xeon(R) CPU E5-2680 v4 @ 2.40GHz |
| cores | 28 |
| memory | 30.31 GiB (32548388864 bytes) |
| node | v24.19.0 |
| platform | linux x64 (7.0.0-30-generic) |
| git commit | `ce93982` |
| date | 2026-09-12T04:35:19.791Z |
| command | `pnpm bench` |
| suite runtime | 29.5 s (fixtures + all cases) |

## Method

- Timing: `process.hrtime.bigint()`, one warmup pass, then an iteration count **calibrated** to a ~40ms slice per case and `rounds` slices per case; the table reports the **median** and the **minimum** per-operation cost, plus ops/s from the median.
- Fixtures: a REAL `DefaultAgentLoop` (composed through `@celestea/runtime`'s `compose()`, driven by `Runtime.runTurn`) generates the first ~1,000 events of every scale over a real `projectingSessionLog(memoryEventStore())`; larger scales replay that engine output turn-by-turn with uniquified ids (see the fixture table). Every measured function is the production body — no stubs.
- Faked at `core` seams only: the `Llm` (an offline scripted stream: a benchmark must not depend on a provider) and the `Tool` set (two `fnTool` closures with realistic schemas).
- The suite is deliberately NOT part of `pnpm check`: it is a measurement harness, not a gate.

## Fixtures

| scale | events | derived messages | estimated tokens | loop events | appended | amplified turns | fixture build |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1k events (1,001) | 1,001 | 572 | 10,153 | 1,001 | 0 | 0 | 133 ms |
| 10k events (10,003) | 10,003 | 5,716 | 102,700 | 1,001 | 9,002 | 1,286 | 102 ms |
| 50k events (50,001) | 50,001 | 28,572 | 514,108 | 1,001 | 49,000 | 7,000 | 165 ms |

## Results

| case | scale | iters | rnd | median ms | min ms | ops/s |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| contextSnapshot() | 1k events (1,001) | 77 | 5 | 0.4175 | 0.4075 | 2395.13 |
| statusline() | 1k events (1,001) | 73 | 5 | 0.6030 | 0.5900 | 1658.51 |
| statusline() [no snapshot] | 1k events (1,001) | 14433 | 5 | 0.0003757 | 0.0003229 | 2661425.81 |
| contextSnapshot() | 10k events (10,003) | 13 | 5 | 4.026 | 3.983 | 248.39 |
| statusline() | 10k events (10,003) | 9 | 5 | 5.745 | 5.633 | 174.08 |
| statusline() [no snapshot] | 10k events (10,003) | 18755 | 5 | 0.0004196 | 0.0003404 | 2383442.00 |
| contextSnapshot() | 50k events (50,001) | 2 | 5 | 25.90 | 20.51 | 38.61 |
| statusline() | 50k events (50,001) | 1 | 5 | 32.61 | 28.01 | 30.66 |
| statusline() [no snapshot] | 50k events (50,001) | 18489 | 5 | 0.0004338 | 0.0003732 | 2305338.31 |
| contextSnapshot() [over budget] | 1k events (1,001) | 4 | 5 | 12.70 | 12.31 | 78.75 |
| contextSnapshot() [over budget] | 10k events (10,003) | 1 | 5 | 1232 | 1217 | 0.81 |
| estimateTokens() ASCII | 4,000 chars | 14789 | 5 | 0.001395 | 0.001379 | 716901.44 |
| estimateTokens() CJK | 4,000 chars | 4642 | 5 | 0.005551 | 0.005355 | 180144.18 |
| estimateMessagesTokens() ASCII | 200 messages | 2599 | 5 | 0.02786 | 0.02509 | 35899.77 |
| estimateMessagesTokens() CJK | 200 messages | 819 | 5 | 0.07194 | 0.07042 | 13899.63 |
| trimContext() [over budget] | 1,000 messages | 1 | 3 | 37.97 | 37.56 | 26.34 |
| trimContext() [over budget] | 2,000 messages | 1 | 3 | 151.4 | 150.8 | 6.60 |
| trimContext() [over budget] | 4,000 messages | 1 | 3 | 599.6 | 599.4 | 1.67 |
| trimContext() [over budget] | 5,000 messages | 1 | 3 | 945.0 | 944.9 | 1.06 |
| trimContext() [disabled] | 28,572 messages | 280 | 5 | 0.2535 | 0.2482 | 3945.07 |
| SessionLog.deriveMessages() | 1k events (1,001) | 294 | 5 | 0.2043 | 0.2019 | 4894.42 |
| SessionLog.events() | 1k events (1,001) | 16371 | 5 | 0.001823 | 0.001805 | 548567.71 |
| SessionLog.deriveMessages() | 10k events (10,003) | 30 | 5 | 2.217 | 2.205 | 451.04 |
| SessionLog.events() | 10k events (10,003) | 2015 | 5 | 0.02537 | 0.02486 | 39422.56 |
| SessionLog.deriveMessages() | 50k events (50,001) | 4 | 5 | 13.00 | 12.21 | 76.95 |
| SessionLog.events() | 50k events (50,001) | 164 | 5 | 0.4365 | 0.4313 | 2291.14 |
| SessionLog.append() replay | 1,000 events | 1097 | 5 | 0.05225 | 0.05159 | 19138.54 |
| SSE frame encode | 1 frame | 12161 | 5 | 0.001004 | 0.0009318 | 996363.33 |
| SSE wire decode | 200 frames | 114 | 5 | 0.4370 | 0.4301 | 2288.08 |
| LoopEvent -> SSE name/payload | 1 event | 61538 | 5 | 0.00006685 | 0.00005928 | 14958078.83 |

## What the numbers say

- **1k events (1,001)** (572 messages, ~10,153 estimated tokens): one `statusline()` tick costs **0.603 ms**, of which **0.417 ms** (69.2%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~0.21% of one core per session.
- **10k events (10,003)** (5,716 messages, ~102,700 estimated tokens): one `statusline()` tick costs **5.745 ms**, of which **4.026 ms** (70.1%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~2.01% of one core per session.
- **50k events (50,001)** (28,572 messages, ~514,108 estimated tokens): one `statusline()` tick costs **32.610 ms**, of which **25.900 ms** (79.4%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~12.95% of one core per session.
- **Estimator口径**: ASCII measures **4 chars/token** (4 bytes/token); CJK measures **1.333 chars/token** (4 bytes/token). A real BPE tokenizer spends roughly one token per CJK character, so `bytes/4` under-counts CJK history by about a quarter — the trim budget and the no-usage-frame statusline estimate inherit that bias for CJK sessions.
- **Trim pass**: over budget, `trimContext()` costs **945.000 ms** at 5,000 messages with a measured log-log growth exponent of **2.00** — the pass re-estimates the whole suffix per candidate cut, so an over-budget session pays that on every statusline tick.
- **Over budget tick**: with a 2,000-token window the same snapshot costs **1232.000 ms** at 10k events (10,003) — the trim pass, not the projection, is the cliff.

## Known noise and limitations

- Single machine, single process, no CPU pinning: absolute numbers move with turbo/thermal state; the **median of 5 slices** plus the min column is what to compare, not a single run.
- The 50k-event tick is allocation/GC dominated (it rebuilds ~28.6k `Message` objects per call): across runs of this same suite it measured 25-39 ms for the same code, so only a change outside that band means anything at that scale.
- The fixtures are synthetic conversations (one tool step + one answer per turn): real sessions mix long tool outputs, retries and compactions, so the per-event cost is representative but the event mix is not universal.
- Amplified scales repeat engine-emitted turns, so a 50k-event log is a replay of ~1,000 real events rather than 50k independently generated ones; the projection/estimate cost depends on the event SHAPES and count, which the amplitude preserves.
- The over-budget trim rows slice a real projected history; the cut-boundary structure (user/assistant alternation) is genuine, but a pathological all-tool history would cut differently.
- UTF-8/4 estimates are reported as measured ratios only: no real tokenizer runs here (no dependency, no network), so the CJK comparison is against the documented BPE behaviour, not against a tokenizer measured on this machine.
- `SSE wire decode` parses one 200-frame document per iteration; per-frame cost assumes frames stay comparable in size.
- Fixture build time includes the loop's own O(history) per-step derivation; it is reported for transparency, never as a measured hot path.

## Re-running and comparing

```bash
# refresh both artifacts (JSON + this document)
pnpm bench

# measure again and print the change against the v2.6.0 baseline (no hard threshold: CI jitter must not fail a build)
pnpm bench -- --compare benchmarks/baseline-v2.6.0.json
```
