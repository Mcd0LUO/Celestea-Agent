# Engine performance baseline — v2.6.0 (W761)

Generated by `pnpm bench` on 2026-09-12T04:55:02.139Z. Machine-readable twin: `celestea-studio-ts.bench-baseline/1` in `benchmarks/baseline-v2.6.1.json`.

The baseline pins the cost of the engine's hot paths so a later regression can be argued with numbers: the W755 statusline tick (which now
performs a full context snapshot per tick), the token estimator and the trim pass, the session-log projection, and SSE envelope encoding/decoding.

## Machine

| fact | value |
| --- | --- |
| CPU | Intel(R) Xeon(R) CPU E5-2680 v4 @ 2.40GHz |
| cores | 28 |
| memory | 30.31 GiB (32548388864 bytes) |
| node | v24.19.0 |
| platform | linux x64 (7.0.0-30-generic) |
| git commit | `d8fadfc` |
| date | 2026-09-12T04:55:02.139Z |
| command | `pnpm bench` |
| suite runtime | 10.2 s (fixtures + all cases) |

## Method

- Timing: `process.hrtime.bigint()`, one warmup pass, then an iteration count **calibrated** to a ~40ms slice per case and `rounds` slices per case; the table reports the **median** and the **minimum** per-operation cost, plus ops/s from the median.
- Fixtures: a REAL `DefaultAgentLoop` (composed through `@celestea/runtime`'s `compose()`, driven by `Runtime.runTurn`) generates the first ~1,000 events of every scale over a real `projectingSessionLog(memoryEventStore())`; larger scales replay that engine output turn-by-turn with uniquified ids (see the fixture table). Every measured function is the production body — no stubs.
- Faked at `core` seams only: the `Llm` (an offline scripted stream: a benchmark must not depend on a provider) and the `Tool` set (two `fnTool` closures with realistic schemas).
- The suite is deliberately NOT part of `pnpm check`: it is a measurement harness, not a gate.

## Fixtures

| scale | events | derived messages | estimated tokens | loop events | appended | amplified turns | fixture build |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1k events (1,001) | 1,001 | 572 | 10,153 | 1,001 | 0 | 0 | 127 ms |
| 10k events (10,003) | 10,003 | 5,716 | 102,700 | 1,001 | 9,002 | 1,286 | 81 ms |
| 50k events (50,001) | 50,001 | 28,572 | 514,108 | 1,001 | 49,000 | 7,000 | 132 ms |

## Results

| case | scale | iters | rnd | median ms | min ms | ops/s |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| contextSnapshot() [assembly] | 1k events (1,001) | 135 | 5 | 0.3564 | 0.3435 | 2806.23 |
| contextSnapshot() [repeat read] | 1k events (1,001) | 10986 | 5 | 0.001593 | 0.001520 | 627611.69 |
| statusline() [cold] | 1k events (1,001) | 116 | 5 | 0.5141 | 0.5110 | 1945.16 |
| statusline() [tick] | 1k events (1,001) | 409 | 5 | 0.1571 | 0.1559 | 6365.46 |
| statusline() [no snapshot] | 1k events (1,001) | 18820 | 5 | 0.0004295 | 0.0003167 | 2328262.50 |
| contextSnapshot() [assembly] | 10k events (10,003) | 18 | 5 | 3.401 | 3.366 | 294.01 |
| contextSnapshot() [repeat read] | 10k events (10,003) | 2752 | 5 | 0.01885 | 0.01866 | 53057.65 |
| statusline() [cold] | 10k events (10,003) | 12 | 5 | 4.903 | 4.822 | 203.98 |
| statusline() [tick] | 10k events (10,003) | 42 | 5 | 1.462 | 1.459 | 684.02 |
| statusline() [no snapshot] | 10k events (10,003) | 17167 | 5 | 0.0002906 | 0.0002846 | 3441543.38 |
| contextSnapshot() [assembly] | 50k events (50,001) | 3 | 5 | 18.26 | 18.03 | 54.78 |
| contextSnapshot() [repeat read] | 50k events (50,001) | 189 | 5 | 0.3483 | 0.3344 | 2871.50 |
| statusline() [cold] | 50k events (50,001) | 1 | 5 | 25.47 | 24.58 | 39.27 |
| statusline() [tick] | 50k events (50,001) | 4 | 5 | 7.911 | 7.652 | 126.41 |
| statusline() [no snapshot] | 50k events (50,001) | 54945 | 5 | 0.0002972 | 0.0002849 | 3364620.11 |
| contextSnapshot() [over budget, assembly] | 1k events (1,001) | 113 | 5 | 0.5112 | 0.5068 | 1956.35 |
| contextSnapshot() [over budget, repeat read] | 1k events (1,001) | 18121 | 5 | 0.001576 | 0.001547 | 634563.69 |
| contextSnapshot() [over budget, assembly] | 10k events (10,003) | 12 | 5 | 4.981 | 4.953 | 200.77 |
| contextSnapshot() [over budget, repeat read] | 10k events (10,003) | 3185 | 5 | 0.01915 | 0.01897 | 52216.95 |
| estimateTokens() ASCII | 4,000 chars | 31463 | 5 | 0.001282 | 0.001275 | 779727.47 |
| estimateTokens() CJK | 4,000 chars | 11558 | 5 | 0.004989 | 0.004937 | 200458.66 |
| estimateMessagesTokens() ASCII | 200 messages | 2238 | 5 | 0.02712 | 0.02653 | 36868.65 |
| estimateMessagesTokens() CJK | 200 messages | 709 | 5 | 0.06692 | 0.06592 | 14942.72 |
| trimContext() [over budget] | 1,000 messages | 106 | 3 | 0.5752 | 0.5359 | 1738.48 |
| trimContext() [over budget] | 2,000 messages | 56 | 3 | 1.090 | 1.086 | 917.54 |
| trimContext() [over budget] | 4,000 messages | 29 | 3 | 2.179 | 2.168 | 458.91 |
| trimContext() [over budget] | 5,000 messages | 22 | 3 | 2.715 | 2.715 | 368.26 |
| trimContext() [disabled] | 28,572 messages | 321 | 5 | 0.2080 | 0.2050 | 4807.24 |
| SessionLog.deriveMessages() | 1k events (1,001) | 356 | 5 | 0.1862 | 0.1831 | 5370.38 |
| SessionLog.events() | 1k events (1,001) | 18472 | 5 | 0.001540 | 0.001511 | 649332.16 |
| SessionLog.deriveMessages() | 10k events (10,003) | 34 | 5 | 1.798 | 1.766 | 556.18 |
| SessionLog.events() | 10k events (10,003) | 3259 | 5 | 0.01901 | 0.01892 | 52613.08 |
| SessionLog.deriveMessages() | 50k events (50,001) | 6 | 5 | 9.904 | 9.671 | 100.97 |
| SessionLog.events() | 50k events (50,001) | 181 | 5 | 0.3425 | 0.3362 | 2919.37 |
| SessionLog.append() replay | 1,000 events | 1221 | 5 | 0.04124 | 0.04044 | 24246.08 |
| SSE frame encode | 1 frame | 22988 | 5 | 0.0008274 | 0.0008024 | 1208639.60 |
| SSE wire decode | 200 frames | 147 | 5 | 0.3770 | 0.3686 | 2652.40 |
| LoopEvent -> SSE name/payload | 1 event | 85592 | 5 | 0.00005216 | 0.00005012 | 19170459.68 |

## What the numbers say

- **1k events (1,001)** (572 messages, ~10,153 estimated tokens): one `statusline()` tick costs **0.514 ms**, of which **0.356 ms** (69.3%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~0.18% of one core per session.
- **10k events (10,003)** (5,716 messages, ~102,700 estimated tokens): one `statusline()` tick costs **4.903 ms**, of which **3.401 ms** (69.4%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~1.70% of one core per session.
- **50k events (50,001)** (28,572 messages, ~514,108 estimated tokens): one `statusline()` tick costs **25.470 ms**, of which **18.260 ms** (71.7%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~9.13% of one core per session.
- **Estimator口径**: ASCII measures **4 chars/token** (4 bytes/token); CJK measures **1.333 chars/token** (4 bytes/token). A real BPE tokenizer spends roughly one token per CJK character, so `bytes/4` under-counts CJK history by about a quarter — the trim budget and the no-usage-frame statusline estimate inherit that bias for CJK sessions.
- **Trim pass**: over budget, `trimContext()` costs **2.715 ms** at 5,000 messages with a measured log-log growth exponent of **0.97** — the pass re-estimates the whole suffix per candidate cut, so an over-budget session pays that on every statusline tick.
- **Over budget tick**: with a 2,000-token window the same snapshot costs **4.981 ms** at 10k events (10,003) — the trim pass, not the projection, is the cliff.

## Known noise and limitations

- Single machine, single process, no CPU pinning: absolute numbers move with turbo/thermal state; the **median of 5 slices** plus the min column is what to compare, not a single run.
- The 50k-event tick is allocation/GC dominated (it rebuilds ~28.6k `Message` objects per call): across runs of this same suite it measured 25-39 ms for the same code, so only a change outside that band means anything at that scale.
- The fixtures are synthetic conversations (one tool step + one answer per turn): real sessions mix long tool outputs, retries and compactions, so the per-event cost is representative but the event mix is not universal.
- Amplified scales repeat engine-emitted turns, so a 50k-event log is a replay of ~1,000 real events rather than 50k independently generated ones; the projection/estimate cost depends on the event SHAPES and count, which the amplitude preserves.
- The over-budget trim rows slice a real projected history; the cut-boundary structure (user/assistant alternation) is genuine, but a pathological all-tool history would cut differently.
- UTF-8/4 estimates are reported as measured ratios only: no real tokenizer runs here (no dependency, no network), so the CJK comparison is against the documented BPE behaviour, not against a tokenizer measured on this machine.
- `SSE wire decode` parses one 200-frame document per iteration; per-frame cost assumes frames stay comparable in size.
- Fixture build time includes the loop's own O(history) per-step derivation; it is reported for transparency, never as a measured hot path.

## Re-running and comparing

```bash
# refresh both artifacts (benchmarks/baseline-v2.6.1.json + this document)
pnpm bench

# measure again and print the change against any baseline JSON (e.g. the historical v2.6.0 one);
# no hard threshold by design: CI jitter must not fail a build
pnpm bench -- --compare benchmarks/baseline-v2.6.1.json --no-write
```
