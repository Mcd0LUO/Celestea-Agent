# Engine performance baseline — v2.7.2 (W761)

Generated by `pnpm bench` on 2026-09-19T08:22:48.475Z. Machine-readable twin: `celestea-studio-ts.bench-baseline/1` in `benchmarks/baseline-v2.7.2.json`.

The baseline pins the cost of the engine's hot paths so a later regression can be argued with numbers: the W755 statusline tick (which now
performs a full context snapshot per tick), the token estimator and the trim pass, the session-log projection, and SSE envelope encoding/decoding.

## Machine

| fact | value |
| --- | --- |
| CPU | Intel(R) Xeon(R) CPU E5-2680 v4 @ 2.40GHz |
| cores | 28 |
| memory | 30.31 GiB (32548388864 bytes) |
| node | v26.8.2 |
| platform | linux x64 (7.0.0-30-generic) |
| git commit | `a7ea4d3` |
| date | 2026-09-19T08:22:48.475Z |
| command | `pnpm bench` |
| suite runtime | 9.1 s (fixtures + all cases) |

## Method

- Timing: `process.hrtime.bigint()`, one warmup pass, then an iteration count **calibrated** to a ~40ms slice per case and `rounds` slices per case; the table reports the **median** and the **minimum** per-operation cost, plus ops/s from the median.
- Fixtures: a REAL `DefaultAgentLoop` (composed through `@celestea/runtime`'s `compose()`, driven by `Runtime.runTurn`) generates the first ~1,000 events of every scale over a real `projectingSessionLog(memoryEventStore())`; larger scales replay that engine output turn-by-turn with uniquified ids (see the fixture table). Every measured function is the production body — no stubs.
- Faked at `core` seams only: the `Llm` (an offline scripted stream: a benchmark must not depend on a provider) and the `Tool` set (two `fnTool` closures with realistic schemas).
- The suite is deliberately NOT part of `pnpm check`: it is a measurement harness, not a gate.

## Fixtures

| scale | events | derived messages | estimated tokens | loop events | appended | amplified turns | fixture build |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1k events (1,001) | 1,001 | 572 | 10,153 | 1,001 | 0 | 0 | 116 ms |
| 10k events (10,003) | 10,003 | 5,716 | 102,700 | 1,001 | 9,002 | 1,286 | 80 ms |
| 50k events (50,001) | 50,001 | 28,572 | 514,108 | 1,001 | 49,000 | 7,000 | 125 ms |

## Results

| case | scale | iters | rnd | median ms | min ms | ops/s |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| contextSnapshot() [assembly] | 1k events (1,001) | 164 | 5 | 0.3683 | 0.3663 | 2715.37 |
| contextSnapshot() [repeat read] | 1k events (1,001) | 13351 | 5 | 0.001518 | 0.001487 | 658767.48 |
| statusline() [cold] | 1k events (1,001) | 94 | 5 | 0.5304 | 0.5213 | 1885.26 |
| statusline() [tick] | 1k events (1,001) | 9826 | 5 | 0.003171 | 0.003086 | 315326.28 |
| statusline() [no snapshot] | 1k events (1,001) | 28612 | 5 | 0.0003351 | 0.0003247 | 2984079.62 |
| contextSnapshot() [assembly] | 10k events (10,003) | 17 | 5 | 3.566 | 3.556 | 280.45 |
| contextSnapshot() [repeat read] | 10k events (10,003) | 2367 | 5 | 0.01836 | 0.01825 | 54473.96 |
| statusline() [cold] | 10k events (10,003) | 11 | 5 | 4.956 | 4.939 | 201.76 |
| statusline() [tick] | 10k events (10,003) | 2106 | 5 | 0.02063 | 0.02047 | 48471.36 |
| statusline() [no snapshot] | 10k events (10,003) | 12348 | 5 | 0.0003450 | 0.0003319 | 2898787.52 |
| contextSnapshot() [assembly] | 50k events (50,001) | 3 | 5 | 18.95 | 17.96 | 52.77 |
| contextSnapshot() [repeat read] | 50k events (50,001) | 192 | 5 | 0.09003 | 0.07784 | 11106.83 |
| statusline() [cold] | 50k events (50,001) | 2 | 5 | 25.88 | 24.76 | 38.64 |
| statusline() [tick] | 50k events (50,001) | 72 | 5 | 0.08651 | 0.08184 | 11559.01 |
| statusline() [no snapshot] | 50k events (50,001) | 19342 | 5 | 0.0003344 | 0.0003293 | 2990384.76 |
| contextSnapshot() [over budget, assembly] | 1k events (1,001) | 99 | 5 | 0.5738 | 0.5718 | 1742.82 |
| contextSnapshot() [over budget, repeat read] | 1k events (1,001) | 14785 | 5 | 0.001508 | 0.001493 | 662964.29 |
| contextSnapshot() [over budget, assembly] | 10k events (10,003) | 10 | 5 | 5.795 | 5.684 | 172.58 |
| contextSnapshot() [over budget, repeat read] | 10k events (10,003) | 2356 | 5 | 0.01842 | 0.01813 | 54275.37 |
| estimateTokens() ASCII | 4,000 chars | 42134 | 5 | 0.0001433 | 0.0001413 | 6979260.83 |
| estimateTokens() CJK | 4,000 chars | 40955 | 5 | 0.0006499 | 0.0006435 | 1538814.03 |
| estimateMessagesTokens() ASCII | 200 messages | 2758 | 5 | 0.01757 | 0.01745 | 56903.22 |
| estimateMessagesTokens() CJK | 200 messages | 1737 | 5 | 0.03547 | 0.03536 | 28191.04 |
| trimContext() [over budget] | 1,000 messages | 101 | 3 | 0.5950 | 0.5946 | 1680.62 |
| trimContext() [over budget] | 2,000 messages | 49 | 3 | 1.206 | 1.196 | 829.32 |
| trimContext() [over budget] | 4,000 messages | 26 | 3 | 2.384 | 2.379 | 419.40 |
| trimContext() [over budget] | 5,000 messages | 20 | 3 | 3.010 | 2.979 | 332.24 |
| trimContext() [disabled] | 28,572 messages | 1038 | 5 | 0.1134 | 0.1106 | 8821.73 |
| SessionLog.deriveMessages() | 1k events (1,001) | 295 | 5 | 0.2031 | 0.2006 | 4922.91 |
| SessionLog.events() | 1k events (1,001) | 15748 | 5 | 0.001442 | 0.001395 | 693562.12 |
| SessionLog.deriveMessages() | 10k events (10,003) | 30 | 5 | 2.016 | 1.992 | 495.96 |
| SessionLog.events() | 10k events (10,003) | 3164 | 5 | 0.01795 | 0.01767 | 55713.02 |
| SessionLog.deriveMessages() | 50k events (50,001) | 4 | 5 | 11.27 | 10.69 | 88.70 |
| SessionLog.events() | 50k events (50,001) | 727 | 5 | 0.07730 | 0.07320 | 12936.02 |
| SessionLog.append() replay | 1,000 events | 1751 | 5 | 0.02755 | 0.02728 | 36296.99 |
| SSE frame encode | 1 frame | 23323 | 5 | 0.0005176 | 0.0004989 | 1932180.41 |
| SSE wire decode | 200 frames | 158 | 5 | 0.3654 | 0.3627 | 2736.43 |
| LoopEvent -> SSE name/payload | 1 event | 50761 | 5 | 0.00005744 | 0.00004776 | 17408854.81 |

## What the numbers say

- **1k events (1,001)** (572 messages, ~10,153 estimated tokens): one `statusline()` tick costs **0.530 ms**, of which **0.368 ms** (69.4%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~0.18% of one core per session.
- **10k events (10,003)** (5,716 messages, ~102,700 estimated tokens): one `statusline()` tick costs **4.956 ms**, of which **3.566 ms** (72%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~1.78% of one core per session.
- **50k events (50,001)** (28,572 messages, ~514,108 estimated tokens): one `statusline()` tick costs **25.880 ms**, of which **18.950 ms** (73.2%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~9.47% of one core per session.
- **Estimator口径**: ASCII measures **4 chars/token** (4 bytes/token); CJK measures **1.333 chars/token** (4 bytes/token). A real BPE tokenizer spends roughly one token per CJK character, so `bytes/4` under-counts CJK history by about a quarter — the trim budget and the no-usage-frame statusline estimate inherit that bias for CJK sessions.
- **Trim pass**: over budget, `trimContext()` costs **3.010 ms** at 5,000 messages with a measured log-log growth exponent of **1.00** — since W762 the pass is a single O(n) suffix-sum walk (it used to re-estimate the whole suffix per candidate cut, exponent 2.00). An over-budget session pays it only when the log CHANGED; an unchanged log hits the W762 cache.
- **Over budget tick**: with a 2,000-token window the same snapshot costs **5.795 ms** at 10k events (10,003) — the trim pass, not the projection, is the cliff.

## Change log

| version | engine change | baseline |
| --- | --- | --- |
| v2.7.2 | **No engine hot-path change** (i18n completion + Windows path fixes). Recorded so the archive has an anchor at the released version. | `benchmarks/baseline-v2.7.2.json` |
| v2.6.2 | **W766**: the statusline's context estimate rides in the W762 snapshot cache (`{request, tokens}` per log state), so an unchanged-log tick is a lookup instead of an O(bytes) walk of the messages. | `benchmarks/baseline-v2.6.2.json` |
| v2.6.1 | **W762**: `trimContext()` de-quadraticised (single-pass suffix sums) + `contextSnapshot()` memoized on the log state; the token rate averages over ACTIVE intervals. | `benchmarks/baseline-v2.6.1.json` |
| v2.6.0 | **W761**: the benchmark suite itself + the W755 context-usage口径 (the statusline now reads the loop's own assembly, which is what made the tick measurable). | `benchmarks/baseline-v2.6.0.json` |

## Known noise and limitations

- Single machine, single process, no CPU pinning: absolute numbers move with turbo/thermal state; the **median of 5 slices** plus the min column is what to compare, not a single run.
- The 50k-event ASSEMBLY rows (`contextSnapshot() [assembly]`, `statusline() [cold]`) are allocation/GC dominated (each rebuilds ~28.6k `Message` objects): they measured 25-39 ms for the same code across runs, so only a change outside that band means anything at that scale. The warmed rows (`[repeat read]`, `[tick]`) allocate nothing and are correspondingly stable.
- The fixtures are synthetic conversations (one tool step + one answer per turn): real sessions mix long tool outputs, retries and compactions, so the per-event cost is representative but the event mix is not universal.
- Amplified scales repeat engine-emitted turns, so a 50k-event log is a replay of ~1,000 real events rather than 50k independently generated ones; the projection/estimate cost depends on the event SHAPES and count, which the amplitude preserves.
- The over-budget trim rows slice a real projected history; the cut-boundary structure (user/assistant alternation) is genuine, but a pathological all-tool history would cut differently.
- UTF-8/4 estimates are reported as measured ratios only: no real tokenizer runs here (no dependency, no network), so the CJK comparison is against the documented BPE behaviour, not against a tokenizer measured on this machine.
- `SSE wire decode` parses one 200-frame document per iteration; per-frame cost assumes frames stay comparable in size.
- Fixture build time includes the loop's own O(history) per-step derivation; it is reported for transparency, never as a measured hot path.

## Re-running and comparing

```bash
# refresh both artifacts (benchmarks/baseline-v2.7.2.json + this document)
pnpm bench

# measure again and print the change against any baseline JSON (e.g. the historical v2.6.0 one);
# no hard threshold by design: CI jitter must not fail a build
pnpm bench -- --compare benchmarks/baseline-v2.7.2.json --no-write
```
