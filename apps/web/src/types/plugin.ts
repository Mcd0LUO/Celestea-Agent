// ============================================================================
// types/plugin.ts — W859：宿主（服务端）插件清单的**宽容**线格式。
// ----------------------------------------------------------------------------
// GET /api/plugins 由后续任务补，本轮只有只读半边：响应的字段结构尚未定型，
// 所以 `plugins` 故意留成 unknown —— 渲染层只认逐个能确认的字符串字段，
// 不认识的结构整项忽略（绝不按猜测的字段名伪造清单）。
// 本文件只有类型，构建期擦除，不进产物。
// ============================================================================

/** GET /api/plugins 的响应（只描述「一定有」的外壳，插件项由渲染层逐项容错）。 */
export interface PluginsResp {
  ok?: boolean;
  /** 宿主插件数组（结构未定型：unknown，渲染层按 string 字段白名单读取）。 */
  plugins?: unknown;
}
