# Engine performance baseline — v2.6.2 (W761)

Generated by `pnpm bench` on 2026-09-12T05:51:50.466Z. Machine-readable twin: `celestea-studio-ts.bench-baseline/1` in `benchmarks/baseline-v2.6.2.json`.

The baseline pins the cost of the engine's hot paths so a later regression can be argued with numbers: the W755 statusline tick (which now
performs a full context snapshot per tick), the token estimator and the trim pass, the session-log projection, and SSE envelope encoding/decoding.

## Machine

| fact | value |
| --- | --- |
| CPU | Intel(R) Xeon(R) CPU E5-2680 v4 @ 2.40GHz |
| cores | 28 |
| memory | 30.31 GiB (32548388864 bytes) |
| node | v24.19.0 |
| platform | linux x64 (7.0.0-30-generic) |
| git commit | `865479c` |
| date | 2026-09-12T05:51:50.466Z |
| command | `pnpm bench` |
| suite runtime | 9.8 s (fixtures + all cases) |

## Method

- Timing: `process.hrtime.bigint()`, one warmup pass, then an iteration count **calibrated** to a ~40ms slice per case and `rounds` slices per case; the table reports the **median** and the **minimum** per-operation cost, plus ops/s from the median.
- Fixtures: a REAL `DefaultAgentLoop` (composed through `@celestea/runtime`'s `compose()`, driven by `Runtime.runTurn`) generates the first ~1,000 events of every scale over a real `projectingSessionLog(memoryEventStore())`; larger scales replay that engine output turn-by-turn with uniquified ids (see the fixture table). Every measured function is the production body — no stubs.
- Faked at `core` seams only: the `Llm` (an offline scripted stream: a benchmark must not depend on a provider) and the `Tool` set (two `fnTool` closures with realistic schemas).
- The suite is deliberately NOT part of `pnpm check`: it is a measurement harness, not a gate.

## Fixtures

| scale | events | derived messages | estimated tokens | loop events | appended | amplified turns | fixture build |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1k events (1,001) | 1,001 | 572 | 10,153 | 1,001 | 0 | 0 | 124 ms |
| 10k events (10,003) | 10,003 | 5,716 | 102,700 | 1,001 | 9,002 | 1,286 | 104 ms |
| 50k events (50,001) | 50,001 | 28,572 | 514,108 | 1,001 | 49,000 | 7,000 | 117 ms |

## Results

| case | scale | iters | rnd | median ms | min ms | ops/s |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| contextSnapshot() [assembly] | 1k events (1,001) | 176 | 5 | 0.3531 | 0.3504 | 2831.83 |
| contextSnapshot() [repeat read] | 1k events (1,001) | 10844 | 5 | 0.001749 | 0.001700 | 571782.99 |
| statusline() [cold] | 1k events (1,001) | 120 | 5 | 0.5255 | 0.5204 | 1902.80 |
| statusline() [tick] | 1k events (1,001) | 7429 | 5 | 0.003394 | 0.003169 | 294680.60 |
| statusline() [no snapshot] | 1k events (1,001) | 22607 | 5 | 0.0003174 | 0.0002951 | 3151087.30 |
| contextSnapshot() [assembly] | 10k events (10,003) | 13 | 5 | 3.373 | 3.318 | 296.47 |
| contextSnapshot() [repeat read] | 10k events (10,003) | 2493 | 5 | 0.02040 | 0.02006 | 49025.19 |
| statusline() [cold] | 10k events (10,003) | 12 | 5 | 4.962 | 4.945 | 201.53 |
| statusline() [tick] | 10k events (10,003) | 2509 | 5 | 0.02315 | 0.02308 | 43199.71 |
| statusline() [no snapshot] | 10k events (10,003) | 14822 | 5 | 0.0003187 | 0.0003077 | 3137739.39 |
| contextSnapshot() [assembly] | 50k events (50,001) | 3 | 5 | 18.50 | 18.37 | 54.05 |
| contextSnapshot() [repeat read] | 50k events (50,001) | 156 | 5 | 0.3836 | 0.3615 | 2606.78 |
| statusline() [cold] | 50k events (50,001) | 1 | 5 | 29.30 | 25.97 | 34.14 |
| statusline() [tick] | 50k events (50,001) | 160 | 5 | 0.3733 | 0.3709 | 2678.98 |
| statusline() [no snapshot] | 50k events (50,001) | 72992 | 5 | 0.0003286 | 0.0003198 | 3043445.74 |
| contextSnapshot() [over budget, assembly] | 1k events (1,001) | 117 | 5 | 0.5404 | 0.5385 | 1850.46 |
| contextSnapshot() [over budget, repeat read] | 1k events (1,001) | 14862 | 5 | 0.001777 | 0.001692 | 562631.10 |
| contextSnapshot() [over budget, assembly] | 10k events (10,003) | 11 | 5 | 5.220 | 5.060 | 191.57 |
| contextSnapshot() [over budget, repeat read] | 10k events (10,003) | 2892 | 5 | 0.02138 | 0.02113 | 46767.84 |
| estimateTokens() ASCII | 4,000 chars | 33407 | 5 | 0.001293 | 0.001288 | 773308.18 |
| estimateTokens() CJK | 4,000 chars | 10974 | 5 | 0.005028 | 0.005015 | 198901.26 |
| estimateMessagesTokens() ASCII | 200 messages | 2146 | 5 | 0.02683 | 0.02676 | 37276.41 |
| estimateMessagesTokens() CJK | 200 messages | 735 | 5 | 0.06635 | 0.06601 | 15070.82 |
| trimContext() [over budget] | 1,000 messages | 99 | 3 | 0.5861 | 0.5853 | 1706.20 |
| trimContext() [over budget] | 2,000 messages | 53 | 3 | 1.177 | 1.176 | 849.85 |
| trimContext() [over budget] | 4,000 messages | 25 | 3 | 2.426 | 2.370 | 412.12 |
| trimContext() [over budget] | 5,000 messages | 20 | 3 | 2.994 | 2.987 | 333.98 |
| trimContext() [disabled] | 28,572 messages | 290 | 5 | 0.2252 | 0.2229 | 4440.80 |
| SessionLog.deriveMessages() | 1k events (1,001) | 314 | 5 | 0.1891 | 0.1873 | 5289.04 |
| SessionLog.events() | 1k events (1,001) | 16565 | 5 | 0.001746 | 0.001727 | 572873.60 |
| SessionLog.deriveMessages() | 10k events (10,003) | 33 | 5 | 1.817 | 1.803 | 550.26 |
| SessionLog.events() | 10k events (10,003) | 84 | 5 | 0.02024 | 0.01909 | 49406.21 |
| SessionLog.deriveMessages() | 50k events (50,001) | 6 | 5 | 10.67 | 10.53 | 93.71 |
| SessionLog.events() | 50k events (50,001) | 162 | 5 | 0.3672 | 0.3665 | 2723.02 |
| SessionLog.append() replay | 1,000 events | 1242 | 5 | 0.04453 | 0.04403 | 22457.53 |
| SSE frame encode | 1 frame | 17160 | 5 | 0.0008235 | 0.0008190 | 1214389.32 |
| SSE wire decode | 200 frames | 146 | 5 | 0.3788 | 0.3742 | 2639.64 |
| LoopEvent -> SSE name/payload | 1 event | 99337 | 5 | 0.00005757 | 0.00005435 | 17369277.55 |

## What the numbers say

- **1k events (1,001)** (572 messages, ~10,153 estimated tokens): one `statusline()` tick costs **0.525 ms**, of which **0.353 ms** (67.2%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~0.18% of one core per session.
- **10k events (10,003)** (5,716 messages, ~102,700 estimated tokens): one `statusline()` tick costs **4.962 ms**, of which **3.373 ms** (68%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~1.69% of one core per session.
- **50k events (50,001)** (28,572 messages, ~514,108 estimated tokens): one `statusline()` tick costs **29.300 ms**, of which **18.500 ms** (63.1%) is the W755 `contextSnapshot()` the tick now performs — at the 2s SSE tick cadence that is ~9.25% of one core per session.
- **Estimator口径**: ASCII measures **4 chars/token** (4 bytes/token); CJK measures **1.333 chars/token** (4 bytes/token). A real BPE tokenizer spends roughly one token per CJK character, so `bytes/4` under-counts CJK history by about a quarter — the trim budget and the no-usage-frame statusline estimate inherit that bias for CJK sessions.
- **Trim pass**: over budget, `trimContext()` costs **2.994 ms** at 5,000 messages with a measured log-log growth exponent of **1.00** — since W762 the pass is a single O(n) suffix-sum walk (it used to re-estimate the whole suffix per candidate cut, exponent 2.00). An over-budget session pays it only when the log CHANGED; an unchanged log hits the W762 cache.
- **Over budget tick**: with a 2,000-token window the same snapshot costs **5.220 ms** at 10k events (10,003) — the trim pass, not the projection, is the cliff.

## Change log

| version | engine change | baseline |
| --- | --- | --- |
| v2.6.2 | **W766**: the statusline's context estimate rides in the W762 snapshot cache (`{request, tokens}` per log state), so an unchanged-log tick is a lookup instead of an O(bytes) walk of the messages. | `benchmarks/baseline-v2.6.2.json` |
| v2.6.1 | **W762**: `trimContext()` de-quadraticised (single-pass suffix sums) + `contextSnapshot()` memoized on the log state; the token rate averages over ACTIVE intervals. | `benchmarks/baseline-v2.6.1.json` |
| v2.6.0 | **W761**: the benchmark suite itself + the W755 context-usage口径 (the statusline now reads the loop's own assembly, which is what made the tick measurable). | `benchmarks/baseline-v2.6.0.json` |

## Known noise and limitations

- Single machine, single process, no CPU pinning: absolute numbers move with turbo/thermal state; the **median of 5 slices** plus the min column is what to compare, not a single run.
- The 50k-event ASSEMBLY rows (`contextSnapshot() [assembly]`, `statusline() [cold]`) are allocation/GC dominated (each rebuilds ~28.6k `Message` objects): they measured 25-39 ms for the same code across runs, so only a change outside that band means anything at that scale. The warmed rows (`[repeat read]`, `[tick]`) allocate nothing and are correspondingly stable.
- The fixtures are synthetic conversations (one tool step + one answer per turn): real sessions mix long tool outputs, retries and compactions, so the per-event cost is representative but the event mix is not universal.
- Amplified scales repeat engine-emitted turns, so a 50k-event log is a replay of ~1,000 real events rather than 50k independently generated ones; the projection/estimate cost depends on the event SHAPES and count, which the amplitude preserves.
- The over-budget trim rows slice a real projected history; the cut-boundary structure (user/assistant alternation) is genuine, but a pathological all-tool history would cut differently.
- UTF-8/4 estimates are reported as measured ratios only: no real tokenizer runs here (no dependency, no network), so the CJK comparison is against the documented BPE behaviour, not against a tokenizer measured on this machine.
- `SSE wire decode` parses one 200-frame document per iteration; per-frame cost assumes frames stay comparable in size.
- Fixture build time includes the loop's own O(history) per-step derivation; it is reported for transparency, never as a measured hot path.

## Re-running and comparing

```bash
# refresh both artifacts (benchmarks/baseline-v2.6.2.json + this document)
pnpm bench

# measure again and print the change against any baseline JSON (e.g. the historical v2.6.0 one);
# no hard threshold by design: CI jitter must not fail a build
pnpm bench -- --compare benchmarks/baseline-v2.6.2.json --no-write
```
