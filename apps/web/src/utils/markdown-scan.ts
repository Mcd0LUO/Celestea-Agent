// ============================================================================
// utils/markdown-scan.ts — 流式 markdown 的**行级正则**（W1485 从 utils/markdown.ts 拆出）
//
// 为什么单独成模块：这些正则是「边界判定」（markdown-region.ts）与「候选区域
// 行内统计」（markdown-region-state.ts）共用的最小原语，两处必须用**同一套口径**
// ——各写一份必然分叉（本仓 W752 的教训）。utils/markdown.ts 受模块体积棘轮
// 约束，新逻辑只能开新文件，故原样搬到这里（正则与注释逐字未变）。
// ============================================================================

// ---- 行级扫描 ----------------------------------------------------------------

/**
 * 围栏开行：与 marked 的 fences 规则同口径——反引号围栏的语言行不得含反引号
 * （marked: `` `{3,}(?=[^`\n]*(?:\n|$)) ``），波浪号围栏语言行不限。
 * 判定必须与 marked 一致：把真围栏误判为普通行会导致在代码块中间固化。
 */
export const FENCE_OPEN_RE = /^ {0,3}(`{3,}(?=[^`\n]*$)|~{3,})([^\n]*)$/;
/**
 * 可能的围栏行（开或闭）：前导同字符 run + 允许的尾随 `~`/`` ` ``。
 * 与 marked 的 fences 规则同口径——闭围栏必须是**同一字符**且长度不短于开行
 * （marked: `(?: {0,3}\1[~`]* *(?=\n|$)|$)`）；若把混字符行当闭行（例如
 * 开行 ``` 而 `~~~` 之类的行），会误在代码块中间固化。
 */
export const FENCE_LINE_RE = /^ {0,3}(`+|~+)([~`]*)[ \t]*$/;
/** 列表项行（无序 / 有序）。 */
export const LIST_RE = /^ {0,3}(?:[-*+]|\d{1,9}[.)])(?:[ \t]|$)/;
/** 「可能是列表项」的残行（`1`、`1.`、`- `、`*`）：下一 tick 补全后会与前一块合并。 */
export const LISTISH_RE = /^ {0,3}(?:[-*+]|\d{1,9})(?:[.)][ \t]*)?$/;
/** 任意缩进深度的列表项行（嵌套列表末行也会与后续同级项合并）。 */
export const LIST_ANY_RE = /^[ \t]*(?:[-*+]|\d{1,9}[.)])(?:[ \t]|$)/;
/** 缩进行（2 空格以上或制表符）：列表/引用续行、缩进代码块可跨空行。 */
export const INDENT_RE = /^(?: {2,}|\t)/;
/** setext 标题下划线（空行理论上阻断，保守起见仍不固化）。 */
export const SETEXT_RE = /^ {0,3}(?:=+|-+)[ \t]*$/;
/** 主题分隔线（`***` 等，行内标记统计前剔除，避免误判未闭合 `**`）。 */
export const THEMATIC_RE = /^ {0,3}(?:(?:\*[ \t]*){3,}|(?:-[ \t]*){3,}|(?:_[ \t]*){3,})$/;
/**
 * 引用定义**单行**判定：`[id]: url`。
 * 只在「非围栏、非缩进代码」行上使用（围栏感知，见文件头 ★ 说明）。
 */
export const REF_DEF_LINE_RE = /^ {0,3}\[[^\]\n]+\]:/;
/** 引用式链接用法：`[text]` 或 `[text][ref]`（未紧跟 `(` 即非行内链接）。 */
export const REF_USE_RE = /\[[^\]\n]*\](?!\()/;
/**
 * 裸 HTML 标签/构造（行内或块级）探测：`<tag`、`</tag>`、`<!--`、`<?`、`<!X`。
 *
 * marked 对 HTML 有多套跨空行吞并规则（容器块 <pre|script|style|textarea>、
 * 注释/PI/CDATA/声明、type 6/7 标签块、行内 raw block 状态），其边界行为依赖
 * 全文结构，无法用廉价的行级统计可靠判定「切分后等价」。因此采取保守策略：
 * 区域内（围栏/缩进代码之外）出现任何裸 HTML 构造 → 该区域不固化，退化为
 * 全量渲染（与现状相同，绝不比现状更差）。围栏/缩进代码里的 `<` 是字面量，
 * 不触发该规则（TS 泛型等不受影响）。
 */
export const RAW_HTML_RE = /<[a-zA-Z!/?]/;
/** 缩进代码行 / 制表符起始行。 */
export const CODE_LINE_RE = /^(?: {4,}|\t)/;

