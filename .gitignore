node_modules/
dist/
coverage/
*.log
.env
.DS_Store
# generated probe evidence is committed on purpose; keep scratch output out
reports/*.tmp

# generated per-run compare output
reports/replay-diff-*.json
results/

# 真实会话导出的 golden fixture 绝不入库（含私人对话）；合成 fixture 用 ! 例外保留
fixtures/sessions/*
!fixtures/sessions/test-*/
!fixtures/sessions/scratch-*/

# Celestea 会话产物（对话日志/附件/授权/检查点）。
# 工作区根可能就是一个 git 仓库（本项目自己就是），会话数据绝不能进版本库。
# W880：canonical 产物已全部移出工作区，落到 $CELESTEA_HOME
#   （<home>/workspaces/<ws>/{sessions,archive,trash,run-code}/prompts.json）。
#
# W887g（收窄）：`.celestea/` 同时是 **project 源层根**（技能 .celestea/skills/ +
# 记忆 .celestea/memory/，见 packages/core/src/celestea-sources.ts），整目录忽略
# 会把「project 层随仓库提交」的设计一起挡掉。因此这里**只忽略会话/运行产物子目录**，
# 放行源层（memory/、skills/ 及其它手写内容）。
.celestea/sessions/
.celestea/archive/
.celestea/trash/
.celestea/run-code/
.celestea/prompts.json
# 凭据（**绝不入库**：本仓是公开的 GitHub 仓）。放本机 git 凭据，供本仓 push 用；
# 目录与文件都是 600/700，且整段忽略 —— 与 docs/AGENT.local.md 同一约定：
# 「本机事实（含凭据）只存本机，真身绝不进版本库」。
.celestea/credentials/
.celestea/credentials
# 更老的根上产物（.celestea-trash/.celestea-archived/.celestea-prompts.json）
.celestea-*
# 最老的根上会话目录（<标题>-<秒>.<纳秒>）
/*-[0-9]*.[0-9]*/

# 构建产物（H 打包）：由 `pnpm run build` 重新生成，与上面的 dist/ 同一约定，绝不入库。
#   · apps/studio/webdist/  —— 前端构建产物，随 @celestea/studio 发布；
#   · packages/core/contracts/ —— 冻结契约的包内副本，随 @celestea/core 发布
#     （装在全局时没有 workspace marker，只能读包内这份）。
# 两者都由 scripts/release-check.mjs 机械断言「新鲜 + 确实进了 tarball」，
# 所以「忘了重新生成」会红，不需要靠提交产物来兜底。
apps/studio/webdist/
packages/core/contracts/

# worker 工作树（W886）：派工时在仓内 <repo>/.worktrees/<wid> 建独立 worktree，
# 而不是 /tmp（这台机的 /tmp 是 tmpfs，node_modules 会常驻内存）。
# 它们必须是 git 与 eslint 的盲区，否则主工作树会把它们当未跟踪文件、并 lint 半成品代码。
.worktrees/

# 本机事实（机器相关：路径/账号/凭据位置/端口）。模板入库，真身绝不入库。
#   AGENT.md 是提交进仓库的协作规范，必须与机器无关；本机差异写在 AGENT.local.md。
docs/AGENT.local.md
